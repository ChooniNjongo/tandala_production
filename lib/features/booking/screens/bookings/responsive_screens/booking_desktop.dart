import 'package:flutter/material.dart';

class BookingsDesktopScreen extends StatelessWidget {
  const BookingsDesktopScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("BookingDesktop"),);
  }
}
